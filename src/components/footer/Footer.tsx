import React from "react";
import st from "./footer.module.scss";

const Footer = () => {
   return (
      <footer>
         <div className={st.footer__wrap}></div>
      </footer>
   );
};

export default Footer;
